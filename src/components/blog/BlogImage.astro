---
import { Image, warnForMissingAlt } from "@astrojs/image/components";

import type { BlogImageProps } from "@lib/blog";

interface Props extends BlogImageProps {}
const { src, width, height, alt, caption, ...attrs } = Astro.props;
// altがなければキャプションを使い, それもなければ適当に設定するが警告を出す.
const resultAlt = alt !== undefined ? alt : caption ? caption : "_";
if (!caption && !alt) {
  warnForMissingAlt();
}
---

<figure>
  <Image
    src={src}
    width={width}
    height={height}
    format="webp"
    alt={resultAlt}
    {...attrs}
  />
  {caption ? <figcaption>{caption}</figcaption> : null}
</figure>

<style>
  figure {
    border: 3px double var(--main-font-color);
    margin: 1.7rem 1rem;
    display: flex;
    flex-direction: column;
    height: fit-content;
  }
  figcaption {
    background-color: rgba(var(--main-font-color-params), 0.8);
    color: var(--main-bg-color);
    padding: 3px;
    text-align: center;
  }
</style>
